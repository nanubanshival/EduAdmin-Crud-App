@model CourseManagement

@{
    ViewData["Title"] = "Add Course";
}

<div class="edu-page-header">
    <div>
        <h1 class="edu-page-title">Add New Course</h1>
        <p class="edu-page-subtitle">Create a new course offering</p>
    </div>
</div>

<div class="edu-card" style="max-width:560px;">
    <div class="edu-card-header">
        <span class="edu-card-title">Course Details</span>
    </div>
    <div class="edu-card-body">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="edu-validation-summary"></div>

            <div class="edu-form-group">
                <label asp-for="CourseName" class="edu-label">Course Name</label>
                <input asp-for="CourseName" class="edu-input" placeholder="e.g. Computer Science" />
                <span asp-validation-for="CourseName" class="edu-field-error"></span>
            </div>

            <div class="edu-form-group">
                <label asp-for="Semester" class="edu-label">Semester</label>
                <select asp-for="Semester" class="edu-select">
                    <option value="">— Select Semester —</option>
                    <option>1st Semester</option>
                    <option>2nd Semester</option>
                    <option>3rd Semester</option>
                    <option>4th Semester</option>
                    <option>5th Semester</option>
                    <option>6th Semester</option>
                </select>
                <span asp-validation-for="Semester" class="edu-field-error"></span>
            </div>

            <div class="edu-form-group">
                <label asp-for="Fees" class="edu-label">Fees (₹)</label>
                <input asp-for="Fees" type="number" step="0.01" class="edu-input" placeholder="15000.00" />
                <span asp-validation-for="Fees" class="edu-field-error"></span>
            </div>

            <div class="edu-form-actions">
                <button type="submit" class="btn-edu-primary">
                    <i class="bi bi-check-lg"></i> Save Course
                </button>
                <a asp-action="Index" class="btn-edu-outline">Cancel</a>
            </div>
        </form>
    </div>
</div>
